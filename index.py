'''
    The raw code is located in the "core/core" directory, 
    while repetitive or big pieces of code are stored in 
    the "utils/utils" directory.

'''

from core.core import generateMessage

if __name__ == "__main__":
    generateMessage()
